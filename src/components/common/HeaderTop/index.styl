.header_top {
  width: 100%;
  position: fixed;
  ms('padding-top', 10px 15px 20px 26px 30px 30px);
  ms('padding-bottom', 10px 15px 20px 26px 30px 30px);
  ms('padding-left', 5px 7px 21px 31px 41px 41px);
  ms('padding-right', 10px 10px 10px 11px 13px 15px);
  top: 0;
  z-index: 1000;

  &.fixed {
    background: #fff;
  }

  &.header_market {
    background: #000;

    .header_content .header_right .other_content .lang_content .lang_text .lang_svg {
      color: #fff;
    }

    &.fixed {
      background: #000;
    }

    .header_content .header_nav .nav_item .nav_top {
      color: #fff;

      &::after {
        background: #fff;
      }

      &.router-link-active {
        color: #fff;
      }
    }

    .wallet_button .base_components_button_layout {
      background: #fff;

      a {
        color: #000;
      }
    }

    .header_content .header_left .nav_wrap {
      .span_box span {
        background: rgba(255, 255, 255, 0.7);
      }

      &.on {
        .span_box span {
          background: #fff;
        }
      }
    }
  }

  .header_content {
    width: 100%;
    position: relative;
    display: flex;
    justify-content: space-between;

    .header_left {
      position: relative;
      display: flex;

      .header_logo {
        ms('width', 56px 56px 66px 66px 76px 76px);
        display: flex;
        align-items: center;

        img {
          width: 100%;
          display: block;
        }
      }

      .nav_wrap {
        padding-left: 5px;
        padding-right: 10px;
        height: 100%;
        right: 0;
        top: 0;
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;

        .span_box {
          height: 16px;
          position: relative;
          width: 22px;

          span {
            width: 22px;
            position: absolute;
            left: 0;
            height: 2px;
            background: rgba(0, 0, 0, 0.7);
            transition();

            &:first-child {
              top: 0;
            }

            &:nth-of-type(2) {
              top: 50%;
              margin-top: -1px;
            }

            &:last-child {
              bottom: 0;
            }
          }
        }

        &.on {
          .span_box {
            span {
              background: #000;

              &:nth-of-type(1) {
                transform: translate3d(0, 7px, 0) rotate(45deg);
              }

              &:nth-of-type(2) {
                opacity: 0;
              }

              &:nth-of-type(3) {
                transform: translate3d(0, -7px, 0) rotate(-45deg);
              }
            }
          }
        }
      }
    }

    .header_nav {
      position: relative;
      display: flex;

      .nav_item {
        padding: 0 16px;
        position: relative;
        cursor: pointer;

        .nav_top {
          width: 100%;
          height: 100%;
          display: flex;
          align-items: center;
          font-size: 14px;
          transition();
          position: relative;
          font-family: 'Source Han Sans CN Regular';

          &:hover, &.on {
            opacity: 0.8;

            .down {
              transform: rotate(180deg);
            }

            &::after {
              opacity: 1;
            }
          }

          &.router-link-active {
            color: #1A1210;

            &::after {
              opacity: 1;
            }
          }

          &.nav4 {
            &::after {
              opacity: 0 !important;
            }
          }

          &::after {
            position: absolute;
            content: '';
            left: 50%;
            bottom: -3px;
            background: #000;
            height: 3px;
            width: 100%;
            transform: translate3d(-50%, 0, 0);
            opacity: 0;
            transition();
          }

          .down {
            transition();
            width: 10px;
            display: inline-block;
            margin-left: 11px;
          }

          .fire {
            width: 13px;
            height: 17px;
            display: inline-block;
            margin-right: 7px;
            margin-top: -3px;
          }
        }

        .sub_nav_item {
          border: 1px solid transparent;
          border-radius: 9px;
          position: absolute;
          left: 0;
          top: 48px;
          width: auto;
          min-width: 160px;
          cs('background', 'base-header-background');
          cs('border-color', 'base-header-border-color');
          z-index: 1;

          a {
            border-top: 1px solid transparent;
            display: block;
            font-size: 14px;
            height: 40px;
            display: flex;
            align-items: center;
            padding: 0 16px;
            cs('border-top-color', 'base-header-border-color');

            &:first-child {
              border-top: none;
            }

            transition();

            &:hover {
              opacity: 0.8;
            }
          }
        }
      }
    }

    .header_right {
      position: relative;
      display: flex;

      .other_content {
        position: relative;
        display: flex;

        .my_nfts {
          ms('margin-right', 0 10px 20px 30px 39px 39px);
          white-space: nowrap;
        }

        .exchange_rate {
          position: relative;
          display: flex;
          align-items: center;
          cursor: pointer;
          margin-right: 9px;
          font-size: 16px;

          span {
            padding: 0 9px;
            display: block;
            word-break: keep-all;
            border-left: 1px solid #636363;
            border-right: 1px solid #636363;
          }
        }

        .lang_content {
          margin-right: 9px;

          .lang_text {
            position: relative;
            display: flex;
            align-items: center;
            font-size: 16px;
            height: 100%;

            img {
              width: 22px;
              margin-right: 9px;
            }

            .lang_svg {
              width: 22px;
              color: #000;
              cursor: pointer;

              svg {
                width: 100%;
              }
            }

            span {
              padding: 0 9px;
              display: block;
              word-break: keep-all;
              border-left: 1px solid #636363;
              border-right: 1px solid #636363;
              cursor: pointer;
            }
          }

          .lang_list {
            border: 1px solid transparent;
            border-radius: 9px;
            position: absolute;
            right: 0;
            top: 48px;
            width: auto;
            min-width: 120px;
            color: #fff;
            background: #000;
            border-color: #2e2e2e;
            z-index: 1;

            a {
              border-top: 1px solid transparent;
              display: block;
              font-size: 14px;
              height: 40px;
              display: flex;
              align-items: center;
              padding: 0 16px;
              color: #fff;
              cs('border-top-color', 'base-header-border-color');

              &:first-child {
                border-top: none;
              }

              transition();

              &:hover {
                opacity: 0.8;
              }
            }
          }
        }

        .theme_content {
          position: relative;
          display: flex;
          align-items: center;
          cursor: pointer;

          img {
            width: 22px;
            height: 22px;
          }
        }
      }
    }
  }

  .small_nav_wrap {
    position: fixed;
    ms('top', 52px 55px 60px 65px 70px 70px);
    left: 0px;
    display: flex;
    flex-direction: column;
    ms('height', calc(100% - 52px) calc(100% - 55px) calc(100% - 60px) calc(100% - 65px) calc(100% - 70px) calc(100% - 70px));
    // padding: 16px 0px;
    width: 236px;
    background-color: #000000;
    z-index: 100;

    .nav_pop_wrap {
      width: 100%;
      height: 100%;
      position: relative;

      .nav_pop_list {
        width: 100%;
        height: 100%;
        position: relative;
        overflow-y: auto;
        overflow-x: hidden;

        // border-top: 0.01rem solid #1f1d22;
        .nav_item {
          width: 100%;
          display: flex;
          align-items: center;
          display: block;
          transition: all 0.3s ease;
          position: relative;

          .nav {
            height: 48px;
            display: flex;
            align-items: center;
            overflow: hidden;
            font-weight: 400;
            font-size: 14px;
            font-family: 'Source Han Sans CN Regular';
            transition: all 0.3s ease;
            position: relative;
            padding: 0 24px;
            font-weight: bold;
            cursor: pointer;
            color: #fff;

            &.router-link-active {
              // color: #000;
              background: #fff;
              background: linear-gradient(90deg, #DFEBFF 0%, #F2FCFD 100%);
              color: #000;

              &.nav4 {
                color: #fff;
                background: none;
              }
            }

            &::after {
              position: absolute;
              content: '';
              top: -50%;
              right: -50%;
              bottom: -50%;
              left: -50%;
              border-bottom: 0.01rem solid rgba(60, 46, 17, 0.5);
              transform: scale(0.5);
            }

            img {
              width: 13px;
              display: block;
              margin-right: 7px;
              margin-top: -3px;
            }
          }

          .top_list {
            width: 100%;
            position: relative;
            height: 48px;
            z-index: 1;
          }

          .sub_list {
            width: 100%;
            position: relative;
            padding-left: 12px;

            .nav {
              &::after {
                border-color: rgba(60, 46, 17, 0.5) !important;
                border-width: 1px 0 !important;
              }

              &.router-link-active {
                // color: #000;
                // background: $base-active-color;
              }
            }

            .nav {
            }
          }

          &.router-link-active {
            &:before {
              content: '';
              position: absolute;
              left: 0;
              top: 0;
              height: 40px;
              width: 100%;
              background: #a28662;
            }

            &:before {
              opacity: 1;
            }

            .nav {
              &::after {
                border-color: #a28662;
                border-width: 0;
              }
            }
          }
        }
      }
    }
  }

  .small_mark {
    position: fixed;
    left: 0px;
    ms('top', 52px 55px 60px 65px 70px 70px);
    width: 100%;
    height: 100%;
    background-color: rgba(10, 22, 43, 0.5);
    z-index: 90;
  }
}

.slidedown-enter-active {
  transition: max-height 1.5s ease-in-out;
}

.slidedown-leave-active {
  transition: max-height 0 ease-in-out;
}

.slidedown-enter-to, .slidedown-leave-from {
  overflow: hidden;
  max-height: 1000px;
}

.slidedown-enter-from, .slidedown-leave-to {
  overflow: hidden;
  max-height: 0;
}

.small_common_list {
  width: 100%;
  position: relative;

  a {
    width: 100%;
    position: relative;
    height: 40px;
    font-size: 14px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #fff;
    border-radius: 8px;

    &.on {
      background: #fff;
      background: linear-gradient(90deg, #dfebff 0%, #f2fcfd 100%);
      color: #000;
    }
  }
}